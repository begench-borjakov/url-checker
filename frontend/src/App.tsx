import './App.css'

function App() {
  return (
    <main className="app">
      <section className="app__content" aria-labelledby="app-title">
        <h1 id="app-title">URL Checker</h1>
        <p>Application shell is ready for feature development.</p>
      </section>
    </main>
  )
}

export default App
